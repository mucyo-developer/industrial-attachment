# Production Requirements for AI Chatbot
Flask==2.3.3
requests==2.31.0
gunicorn==21.2.0
python-dotenv==1.0.0

# Optional: For production database (if needed in future)
# psycopg2-binary==2.9.7
# redis==5.0.1

# Security and monitoring
# prometheus-flask-exporter==0.22.4
